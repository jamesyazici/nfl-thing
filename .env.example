# Repo-root env reference (documentation only — the frontend reads js/config.js,
# not this file; Edge Functions read supabase/functions/.env, not this file).
# See README "Environment variables" for exactly where each value goes.

CURRENT_SEASON=2026
PREDICTION_MARKET_PROVIDER=auto
