// Copy this file to js/config.js and fill in your own project's values.
// js/config.js is gitignored — never commit real values there.
//
// Both of these are PUBLIC values (safe to ship in a GitHub Pages bundle)
// as long as Row Level Security is correctly configured on every table —
// see README "Environment variables" for exactly where each one comes from.
export const SUPABASE_URL = 'https://YOUR_PROJECT_REF.supabase.co';
export const SUPABASE_PUBLISHABLE_KEY = 'sb_publishable_XXXXXXXXXXXXXXXXXXXX';
